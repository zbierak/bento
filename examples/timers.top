/* 
 * First all nodes need to be defined along 
 * with their default addresses. BTW, look, 
 * now nodes can take different roles!
 */
nodes {
	node1(localhost:2013) is SORTER,
	node2(localhost:2014) is SORTER
}


/*
 * Creates an all-to-all link between selected 
 * nodes using their default addresses.
 */
a2a {
	node1, node2
}
